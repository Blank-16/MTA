# Backend connection (server-side only — never exposed to browser)
BACKEND_URL=http://backend:8000
INTERNAL_API_KEY=replace-with-random-secret

# Database (BFF session tracking only)
DATABASE_URL=postgresql://triage_user:changeme@postgres:5432/triage_db

# Public vars (safe to expose to browser)
NEXT_PUBLIC_APP_URL=http://localhost:3000
